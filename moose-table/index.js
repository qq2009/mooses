export { default as MTable } from './index.vue';

/**
 * 定义列配置
 * @param {Array<TableColumnsConfig>} columns
 * @returns {Array<TableColumnsConfig>}
 */
export function defineTableColumns(columns) {
    return columns;
}

export { default as MForm } from './index.vue';

/**
 * 定义表单配置
 *
 * @param {Array<FormConfig>} columns 列配置数组
 * @returns {Array<FormConfig>} 返回列配置数组
 */
export function defineFormColumns(columns) {
    return columns;
}
